//import React from 'react';
//import { mount } from 'enzyme';
//import RunModel from './components/RunModel';

describe('Mounted RunModel', () => {
  // FIXME: need to add new tests for the new RunModel
  //const wrapper = mount(<RunModel />);
  it('should render Population Graph on Click', () => {
    // const button = wrapper.find('btn-success').first();
    // button.simulate('click');
    // const state = wrapper.state('loadingSourceCode');
    // expect(state).toEqual(false);
    // FIXME: need to add new test for the new RunModel
    //const state = wrapper.state('loadingSourceCode');
    //expect(state).toEqual(false);
  });
});
